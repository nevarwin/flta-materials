import 'package:flutter/material.dart';

class GroceryItemScreen extends StatelessWidget {
  const GroceryItemScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
